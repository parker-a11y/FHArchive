# Change archive keyboard shortcuts

Update the global shortcuts in the archive navigation:

- **New Record / Quick Entry:** `Ctrl+Option+F12` (`⌃⌥F12`)
- **All Records:** `Ctrl+Option+F11` (`⌃⌥F11`)
- Match the physical function keys so keyboard layout does not affect detection.
- Keep shortcuts disabled while typing in form fields.
- Keep New Record restricted to users with edit permission.
- Update the sidebar shortcut labels to show the new combinations.

## Verify

Confirm both shortcuts navigate correctly and the preview build passes.
